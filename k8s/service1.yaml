apiVersion: apps/v1
kind: Service
metadata:
  name: nginx-app
	labels:
    app: nginx-app  
spec:
  ports:
	  - port: 80
	    protocol: TCP
		  targetPort: 8080
	selector:
	  app: nginx-app